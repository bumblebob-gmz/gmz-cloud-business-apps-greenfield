# Grafana — set a strong password, never use default
GRAFANA_ADMIN_PASSWORD=changeme_please

# Alertmanager SMTP — required for alert delivery
ALERTMANAGER_SMTP_FROM=alerts@irongeeks.eu
ALERTMANAGER_SMTP_HOST=smtp.example.com
ALERTMANAGER_SMTP_PORT=587
ALERTMANAGER_SMTP_USER=alerts@irongeeks.eu
ALERTMANAGER_SMTP_PASS=

# Ops team email receiving all alerts
ALERTMANAGER_OPS_EMAIL=ops@irongeeks.eu
