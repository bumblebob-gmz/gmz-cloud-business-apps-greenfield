name: IaC Security Lint

on:
  push:
    branches: ["main"]
    paths:
      - "infra/opentofu/**"
      - ".github/workflows/iac-security-lint.yml"
  pull_request:
    paths:
      - "infra/opentofu/**"
      - ".github/workflows/iac-security-lint.yml"

jobs:
  checkov:
    name: "IaC security lint (checkov)"
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4- name: Run checkov on infra/opentofu/
        uses: bridgecrewio/checkov-action@f9b0a2206b0401cad02ac0a66be2a7934a5be838 # v12with:
          directory: infra/opentofu
          framework: terraform
          # Fail the job on HIGH or CRITICAL severity findings only
          soft_fail_on: LOW,MEDIUM
          # Output formats: produce SARIF for GitHub Security tab + CLI summary
          output_format: cli,sarif
          output_file_path: console,checkov-results.sarif
          # Skip checks that don't apply to local OpenTofu/Proxmox context
          skip_check: >-
            CKV_TF_1,
            CKV2_GHA_1

      - name: Upload SARIF to GitHub Security tab
        if: always()
        uses: github/codeql-action/upload-sarif@820e3160e279568db735cee8ed8f8e77a6da7818 # v3with:
          sarif_file: checkov-results.sarif
          category: checkov-iac
