---
# SSH source restriction.
# Set to the management VM's IP or CIDR (e.g. "10.0.0.5" or "10.0.0.0/24").
# When non-empty, UFW will only allow SSH from this source.
# Leave empty to allow SSH from anywhere (less secure — only for initial bootstrap).
ssh_allowed_source: ""
